# Runtime
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Service identity
SERVICE_NAME=zk-inference-monitor

# Add integration-specific variables below as they become required
# (e.g. RPC endpoints, API keys, database URLs). Do not commit real
# values — this file only documents the shape of the configuration.
